{
  "name": "mobile-arcade-captive-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prod": "browserify -i index.js -o static/bundle.js && node server",
    "start": "npm-run-all -p start:*",
    "start:bundle": "watchify index.js -o static/bundle.js",
    "start:server": "DEBUG=socket.io:server node server"
  },
  "dependencies": {
    "@hughsk/fulltilt": "^0.7.1",
    "browserify": "^16.1.0",
    "express": "^4.16.2",
    "npm-run-all": "^4.1.2",
    "socket.io": "^2.0.4",
    "socket.io-client": "^2.0.4",
    "touches": "^1.2.2",
    "watchify": "^3.11.0"
  }
}
